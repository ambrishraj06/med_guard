# MedGuard — core dependencies (deploy-safe, minimal)
streamlit>=1.40
groq>=0.11

# --- Optional cross-checker models (NOT required by the core audit engine) ---
# The app runs fully without these; the sidebar dropdown simply lists them as
# unavailable. Enable only on hosts with >=2GB RAM (e.g. HF Spaces cpu-basic):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install "transformers<5"     # HHEM requires 4.x (v5 breaks its custom code)
#   pip install accelerate
#   pip install "minicheck @ git+https://github.com/Liyan06/MiniCheck.git"
