# Copy this file to secrets.toml and paste YOUR key (get one free at https://console.groq.com/keys)
# secrets.toml is git-ignored — it will never be committed.
GROQ_API_KEY = "gsk_paste_your_new_key_here"
